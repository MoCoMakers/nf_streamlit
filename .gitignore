app/.streamlit/secrets.toml
app/.streamlit/config.toml
.idea/
.DS_Store
__pycache__/
data/
venv/

reference/*
references/

tools.yaml
.mcp.json
scripts/config.yaml
# How to check all the files that will be ignored
# find . -not -path './.git/*' | git check-ignore --stdin -v

# Runtime logs
logs/

# Session journal notes (internal dev notes, not for public repo)
docs/sessions/

# GMM research assets (heavy binaries + nested clone)
analysis/GMM/docs-for-ai/PDFs/
analysis/GMM/reference/

# Local infra config (credentials)
infra/config.yaml

# Kestra local secret fills (never commit real passwords)
kestra/secrets.env

# Pipeline scripts parked here for local reference while kestra/scripts/ is authoritative
scripts/deprecated/

# Scratch / API error logs
ns_upload.txt

.claude/
docs-for-ai/*